/**
 * 본문이 내려가지 않는 후기(CompanyReview.hiddenReason)의 자리표시 문구.
 *
 * 후기를 지우지 않고 자리를 남기는 것이 이 기능의 요지다 — 후기 수가 조용히 줄면 읽는 쪽에서는
 * "원래 없었다"와 구분되지 않아, 불리한 후기가 사라진 자리를 알아볼 수 없다. 그래서 왜 비었는지를
 * 그 자리에서 말한다.
 *
 * 두 사유를 한 문구로 합치지 않는다. 협의 비공개는 작성자가 동의한 결과이고 삭제는 운영정책 위반이라,
 * 읽는 쪽이 후기의 신뢰도를 판단할 때 서로 다른 정보다.
 */
export const REVIEW_HIDDEN_NOTICE: Record<"agreement" | "violation", string> = {
  agreement: "약국의 요청과 작성자 확인을 거쳐 비공개 처리된 후기입니다.",
  violation: "운영정책 위반으로 삭제된 후기입니다.",
};
