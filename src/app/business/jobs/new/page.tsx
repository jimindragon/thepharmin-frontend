import { TrackSelectionClient } from "./TrackSelectionClient";

export const metadata = {
  title: "공고 등록 · THE PHARMA Recruit",
};

export default function BusinessNewJobPage() {
  return <TrackSelectionClient />;
}
